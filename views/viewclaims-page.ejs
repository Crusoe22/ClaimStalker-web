<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Claim Status</title>
    <link rel="stylesheet" href="/viewclaims-page.css">
    <link rel="icon" type="image/jpg" href="/images/Imagecrop2.jpg">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>

<body>
    <%- include('sidebar') %>

    <main class="page-content">
        <h1 class="page-title">Check Your Claim Status</h1>

        <div class="search-container">
            <form action="/view-claim" method="GET" class="search-form">
                <label for="policyNumber" class="search-label">Enter Policy Number:</label>
                <input type="text" id="policyNumber" name="policyNumber" required placeholder="e.g., A1B2-C3D4-E5F6" class="search-input">
                <button type="submit" class="search-button">
                    <i class="fas fa-search"></i> Search
                </button>
            </form>
        </div>

        <% if (claim) { %>
            <div class="claim-details-container">
                <h2 class="section-title">Claim Details for Policy #<%= claim.policyNumber %></h2>
                <div class="claim-info-grid">
                    <div class="info-card">
                        <i class="fas fa-user-circle info-icon"></i>
                        <h3 class="info-label">Contact Information</h3>
                        <p><strong>Name:</strong> <%= claim.name %></p>
                        <p><strong>Email:</strong> <%= claim.email %></p>
                        <p><strong>Phone:</strong> <%= claim.phone %></p>
                    </div>
                    <div class="info-card">
                        <i class="fas fa-building info-icon"></i>
                        <h3 class="info-label">Policy & Company</h3>
                        <p><strong>Policy Number:</strong> <%= claim.policyNumber %></p>
                        <p><strong>Insurance Company:</strong> <%= claim.insuranceCompany %></p>
                    </div>
                    <div class="info-card">
                        <i class="fas fa-calendar-alt info-icon"></i>
                        <h3 class="info-label">Loss Information</h3>
                        <p><strong>Date of Loss:</strong> <%= claim.claimDate.toDateString() %></p>
                        <p><strong>Location:</strong> <%= claim.location %></p>
                        <% if (claim.autoLoss) { %>
                            <p><strong>Auto Loss:</strong> Yes</p>
                        <% } %>
                        <% if (claim.propertyLoss) { %>
                            <p><strong>Property Loss:</strong> Yes</p>
                        <% } %>
                    </div>
                    <div class="info-card full-width-card">
                        <i class="fas fa-info-circle info-icon"></i>
                        <h3 class="info-label">Description of Loss</h3>
                        <p><%= claim.description %></p>
                    </div>
                </div>
            </div>
        <% } else if (typeof claim !== 'undefined') { %>
            <div class="no-results-message">
                <i class="fas fa-exclamation-triangle"></i>
                <p>No claim was found with that policy number. Please double-check the number and try again.</p>
            </div>
        <% } %>
    </main>
</body>
</html>